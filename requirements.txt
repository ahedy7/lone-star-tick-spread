# Lone star tick range-expansion pipeline -- pinned for reproducibility.
# Resolved against Python 3.13 on 2026-06-03. Recreate with:
#   python -m venv .venv
#   .venv/Scripts/activate        (Windows)  |  source .venv/bin/activate (*nix)
#   pip install -r requirements.txt

# --- Core data acquisition / geospatial ---
pygbif==0.6.6
geopandas==1.1.3
pandas==3.0.3
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.12.1
requests==2.34.2
requests-cache==1.3.2
matplotlib==3.10.9
numpy==2.4.6

# --- Config / secrets ---
python-dotenv==1.2.2

# --- Notebook tooling (Stage 1 sanity report) ---
jupyter==1.1.1
ipykernel==7.2.0
nbconvert==7.17.1
nbformat==5.10.4
