# Lone star tick range-expansion pipeline -- pinned for reproducibility.
# Resolved against Python 3.13 on 2026-06-03. Recreate with:
#   python -m venv .venv
#   .venv/Scripts/activate        (Windows)  |  source .venv/bin/activate (*nix)
#   pip install -r requirements.txt

# --- Core data acquisition / geospatial ---
pygbif==0.6.6
geopandas==1.1.3
pandas==3.0.3
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.12.1
h3==4.5.0
requests==2.34.2
requests-cache==1.3.2
matplotlib==3.10.9
numpy==2.4.6
scipy==1.16.3

# --- Stage 4 visualization (static figures: real tile basemaps + GIF) ---
# The interactive map (viz/) has NO Python dependency -- it loads deck.gl,
# MapLibre and h3-js from CDNs and reads the local exported data bundle.
contextily==1.7.0   # CARTO Positron tile basemap under the static hex maps
pillow==12.0.0      # PillowWriter backend for the animated GIF export

# --- Config / secrets ---
python-dotenv==1.2.2

# --- Notebook tooling (Stage 1 sanity report) ---
jupyter==1.1.1
ipykernel==7.2.0
nbconvert==7.17.1
nbformat==5.10.4
